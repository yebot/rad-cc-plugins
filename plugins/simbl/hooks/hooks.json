{
  "hooks": {
    "PostToolUse": [
      {
        "filter": "Bash",
        "command": "bash",
        "args": [
          "-c",
          "if echo \"$TOOL_OUTPUT\" | grep -qiE '(passed|success|0 failed|All tests passed|PASS)'; then echo 'simbl: Tests passing - consider updating task status with simbl done <id>'; fi"
        ]
      }
    ]
  }
}
